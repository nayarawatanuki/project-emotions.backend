const path = require('path');

module.exports = {
    config: path.resolve(__dirname, 'src', 'api', 'connection', 'db.js'),
    'migrations-path': path.resolve(__dirname, 'src', 'api', 'db', 'migrations'),
};